# Phase C Failure Ledger

| ID | Stage | Description | Actual Behavior | Root Cause | Fix | Status |
|----|-------|-------------|-----------------|------------|-----|--------|
| 1  | C2 | Data Intelligence heading locator strict violation | Locator matched multiple items (`h1` and `h2`) | Playwright test wrote ambiguous locator. | Used `.first()` on `getByRole`. | RESOLVED |
| 2  | C2 | Upload dataset button not clicked | File was attached to input but submit button wasn't clicked | Test omitted clicking the upload button. | Added `.click()` on "Upload dataset" button. | RESOLVED |
| 4 | C2 | File Upload 409 Conflict | Playwright test hung again | Backend returned 409 Conflict because `e2e-tenant-123` already uploaded the dataset in the previous failed run. | Used `Date.now()` to generate a unique `x-tenant-id` for each test run. | RESOLVED |
| 5 | C2 | File Upload 422 Unprocessable Entity | Playwright test hung again | Appending a comment `# unique` to the CSV broke the backend CSV parsing. | Appended a valid CSV row instead. | RESOLVED |
| 6 | C3 | Schema Confirmation timeout | Playwright test hung again | The schema workspace requires running inference before the confirm mapping button is visible, and the confirm mapping button opens a modal. | Added explicit clicks for `Run inference`, `Confirm mapping`, and `Confirm`. | RESOLVED |
| 7 | C3 | Schema Confirmation strict locator violation | Playwright test hung again | `getByRole('button', { name: /Confirm/i })` matched "Confirm mapping" which was blocked by the dialog overlay. | Added `exact: true` to the `Confirm` button locator. | RESOLVED |
| 8 | C4 | Quality Evaluation locator mismatch | Playwright test hung again | Looked for `Run Evaluation` but actual UI label was `Run analysis`. | Updated the locator in the test script to `Run analysis`. | RESOLVED |
| 9 | C4 | Quality Evaluation endpoint mismatch | Playwright test hung again | Looked for `/quality/evaluate` intercept but actual backend endpoint is `/quality/analyze`. | Updated the locator in the test script to `/quality/analyze`. | RESOLVED |
| 10 | C5 | Preparation Endpoint / Locator Mismatch | Playwright test hung again | The preparation button label was changed to `Start preparation` instead of `Create Prepared` in the UI, and the Target / Date comboboxes needed to be selected before the button becomes enabled. | Updated the locator and added selectOption to fill required form fields. | RESOLVED |
| 11 | C5 | Preparation failed with 409 Conflict | Playwright test hung again | The dynamic CSV fixture appended `2025-12-31`, creating an 8-month gap. The chronological splitting algorithm put the Validation and Test splits in the empty months, dropping them due to missing targets, and raising a `Chronological split does not meet configured minimum row counts` error. | Changed the appended row date to `2025-04-30` so it fits within the existing temporal span. | RESOLVED |
| 12 | C5 | Preparation failed with 409 Conditional quality override | Playwright test hung again | The backend rejected preparation because the dataset had conditional quality warnings, but the frontend was missing the UI for `quality_override` and `quality_override_reason`. | Added the missing UI fields to `preparation-workspace.tsx` and added playwright steps to check the box and provide a reason. | RESOLVED |
| 13 | C5 | E2E Timeout Exceeded | Playwright test hung at dataset upload due to slow Next.js compilation after server restart combined with a 15-second timeout. | Increased the `full-workflow.spec.ts` timeout to 60000ms. | RESOLVED |
| 15 | C6-C9 | Incomplete E2E Coverage | The E2E script previously ended at forecasting without navigating to the correct workspace and triggering model training, explainability, operations analytics, or AI Copilot. | Added steps to navigate to `/data-intelligence/.../forecast`, trigger `Run baseline and advanced experiment`, and navigate to Explainability, Operations Analytics, and AI Copilot pages. Increased timeout to 90 seconds. | RESOLVED |
